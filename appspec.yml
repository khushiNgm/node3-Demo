version: 0.0
os: linux

hooks:
  BeforeInstall:
    - location: scripts/permission.sh
      timeout: 300
      runas: root

  AfterInstall:
    - location: scripts/deploy.sh
      timeout: 300
      runas: root